# 服务端口
server:
  port: 10000
  # 服务名
spring:
  application:
    name: ty-canal-client

  # 环境设置：dev、test、prod
  profiles:
    active: dev

  # mysql数据库连接
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ty_edu?serverTimezone=GMT%2B8
    username: root
    password: 123456